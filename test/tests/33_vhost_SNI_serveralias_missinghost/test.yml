!connection
gnutls_params:
  - x509cafile=authority/x509.pem
  - sni-hostname=example.virtual.host
actions:
  - !raw10
    method: GET
    path: /test.txt
    headers:
      Connection: close
    expect:
      status: 421
      body:
        contains:
          - 'Misdirected Request'
          - 'Server Name Indication (SNI)'
