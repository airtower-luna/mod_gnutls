!connection
gnutls_params:
  - x509cafile=authority/x509.pem
actions:
  - !request
    path: /
    expect:
      status: 404
  - !request
    method: GET
    path: /test.txt
    expect:
      status: 200
      body: |
        test
