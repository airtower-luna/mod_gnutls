<IfModule mod_gnutls.c>

  # The default method is to use a shared memory backed cache using
  # mod_socache_shmcb. Other mod_socache implementations can be used if
  # desired.
  GnuTLSCache shmcb:cache/gnutls_cache(65536)

</IfModule>
